query GetMatchedUsersWithoutConversation($pageInput: PageInput!) {
  getMatchedUsersWithoutConversation(pageInput: $pageInput) {
    matchers {
      id
      name
      picture
    }
    info {
      count
      next
      pages
      prev
    }
  }
}
